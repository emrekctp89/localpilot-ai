# ─── Vercel (frontend) ───────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
NEXT_PUBLIC_AI_SERVICE_URL=https://localpilot-ai-service.onrender.com

# Opsiyonel: client-side hata raporlama (Sentry ingest veya webhook URL)
# NEXT_PUBLIC_ERROR_REPORT_URL=https://your-ingest-endpoint

# ─── Render / Railway (ai-service) ─────────────────────────
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
FRONTEND_URL=https://localpilot-ai-1b2h.vercel.app
ALLOWED_ORIGINS=https://localpilot-ai-1b2h.vercel.app,https://localpilot-ai-*.vercel.app
# Wildcard entries auto-map to allow_origin_regex; override if needed:
# ALLOW_ORIGIN_REGEX=https://.*\.vercel\.app
# CORS_ALLOW_VERCEL_PREVIEWS=true
ENVIRONMENT=production
AI_SERVICE_REQUIRE_AUTH=true
RATE_LIMIT_PER_MINUTE=60

# ─── Stripe webhook ──────────────────────────────────────────
# Endpoint: https://localpilot-ai-service.onrender.com/stripe-webhook
# Stripe Dashboard → Webhooks → Events: checkout.session.completed
# Doğrulama: curl https://localpilot-ai-service.onrender.com/health
# Beklenen: checks.stripe=true (STRIPE_* env set ise)

# ─── Supabase migrations (SQL Editor sırasıyla) ──────────────
# 001_operational_tables.sql
# 002_core_rls.sql
# 003_campaigns_content.sql
# 004_crm_activities.sql
# 005_ai_usage.sql
# 006_platform.sql

# ─── GitHub Actions secrets (E2E) ────────────────────────────
# E2E_TEST_EMAIL=
# E2E_TEST_PASSWORD=
# E2E_TEST_HAS_BUSINESS=true
# E2E_PUBLIC_BUSINESS_ID=

# ─── Ajans modu (manuel) ─────────────────────────────────────
# profiles.role = 'agency' → çoklu işletme switcher aktif olur