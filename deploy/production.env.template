# ─── Vercel (frontend) ───────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
# Production app URL (middleware platform host ayrımı + canonical)
NEXT_PUBLIC_APP_URL=https://localpilot-ai-1b2h-phi.vercel.app
# Render dashboard → Service URL (ör. localpilot-ai-1eea.onrender.com)
NEXT_PUBLIC_AI_SERVICE_URL=https://localpilot-ai-1eea.onrender.com

# Faz G.3 — custom domain (server-only; Vercel → Account → Tokens)
# VERCEL_TOKEN=vercel_xxxx
# VERCEL_PROJECT_ID=prj_xxxx
# VERCEL_TEAM_ID=team_xxxx   # opsiyonel
# PLATFORM_HOSTS=custom-primary.example.com  # ekstra app host’ları (virgülle)

# Opsiyonel: client-side hata raporlama (Sentry ingest veya webhook URL)
# NEXT_PUBLIC_ERROR_REPORT_URL=https://your-ingest-endpoint

# ─── Render / Railway (ai-service) ─────────────────────────
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# Test aşamasında sk_test_ kullanın (gerçek kart gerekmez). Canlıya geçince sk_live_.
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Faz F — Stripe Dashboard → Products → Pro → recurring prices (TRY)
STRIPE_PRICE_ID_MONTHLY=price_...
STRIPE_PRICE_ID_YEARLY=price_...
FRONTEND_URL=https://localpilot-ai-1b2h-phi.vercel.app
ALLOWED_ORIGINS=https://localpilot-ai-1b2h-phi.vercel.app,https://localpilot-ai-*.vercel.app
# Wildcard entries auto-map to allow_origin_regex; override if needed:
# ALLOW_ORIGIN_REGEX=https://.*\.vercel\.app
# CORS_ALLOW_VERCEL_PREVIEWS=true
ENVIRONMENT=production
AI_SERVICE_REQUIRE_AUTH=true
RATE_LIMIT_PER_MINUTE=60

# Owner lead notify (optional — mini site lead → e-posta / WhatsApp Cloud)
# RESEND_API_KEY=re_xxxx
# RESEND_FROM_EMAIL=LocalPilot <onboarding@resend.dev>
# WHATSAPP_ACCESS_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=

# ─── Stripe (test modu) ──────────────────────────────────────
# Test kart: 4242 4242 4242 4242 | SKT: gelecek tarih | CVC: herhangi 3 hane
# Dashboard: Test mode açık → Developers → API keys → sk_test_...
# Webhook (Render): https://localpilot-ai-service.onrender.com/stripe-webhook
#   Events: checkout.session.completed
# Yerel webhook: stripe listen --forward-to localhost:8000/stripe-webhook
# Health doğrulama: stripe_mode=test olmalı (sk_test_ kullanılıyorsa)

# ─── Supabase migrations (SQL Editor sırasıyla) ──────────────
# 001_operational_tables.sql
# 002_core_rls.sql
# 003_campaigns_content.sql
# 004_crm_activities.sql
# 005_ai_usage.sql
# 006_platform.sql
# 007_fix_rls_recursion.sql
# 008_schema_migrations.sql

# ─── GitHub Actions Secrets (opsiyonel schema verify) ───────
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# ─── GitHub Actions secrets (E2E) — see docs/ops-e2e.md ──────
# E2E_TEST_EMAIL=
# E2E_TEST_PASSWORD=
# E2E_TEST_HAS_BUSINESS=true
# E2E_PUBLIC_BUSINESS_ID=
# Panel-core (Finans/CRM/Ayarlar) requires HAS_BUSINESS=true

# ─── Pro üyelik (manuel — güncel) ───────────────────────────
# Supabase SQL Editor:
#   UPDATE profiles SET is_pro = true, pro_activated_at = NOW() WHERE id = '<uuid>';
# Otomatik Stripe aktivasyonu ileride (Faz A) yeniden açılabilir.

# ─── GitHub Actions Variables (production monitoring) ────────
# PRODUCTION_MONITORING_ENABLED=true
# AI_SERVICE_URL=https://localpilot-ai-1eea.onrender.com
# FRONTEND_URL=https://your-app.vercel.app

# ─── GitHub Actions Variables (Faz D sector cron) ────────────
# SECTOR_AUTOMATION_CRON_ENABLED=true
# Requires secrets: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY

# ─── Migration doğrulama (Faz B) ─────────────────────────────
# Supabase SQL Editor → supabase/scripts/verify_schema.sql

# ─── Faz E — WhatsApp Cloud API (Render ai-service) ─────────
# WHATSAPP_ACCESS_TOKEN=          # Meta Business System User token
# WHATSAPP_PHONE_NUMBER_ID=       # Cloud API phone number id
# WHATSAPP_GRAPH_API_VERSION=v21.0

# ─── Faz E — Google Business Profile OAuth (Render ai-service) ─
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_REDIRECT_URI=https://localpilot-ai-service.onrender.com/integration/google/oauth/callback

# ─── Legacy dual-read (Faz C) ────────────────────────────────
# Operasyonel tablolar doluysa JSON fallback'i kapatın:
# NEXT_PUBLIC_DISABLE_LEGACY_DUAL_READ=true

# ─── Ajans modu (manuel) ─────────────────────────────────────
# profiles.role = 'agency' → çoklu işletme switcher aktif olur

# ─── Faz F — Partner program (Supabase migration 010) ────────
# 010_partner_program.sql → partner_profiles, referral_attributions, commission_ledger
# Referans linki: /auth?ref=LP-XXXXXX
# Ajans komisyonu %20 · Referans komisyonu %10
# Komisyon onayı (panel): profiles.commission_admin = true (011 migration)
#   UPDATE profiles SET commission_admin = true WHERE id = '<admin-uuid>';