# ─── Vercel (frontend) ───────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
NEXT_PUBLIC_AI_SERVICE_URL=https://localpilot-ai-service.onrender.com

# ─── Render / Railway (ai-service) ─────────────────────────
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
FRONTEND_URL=https://your-app.vercel.app
ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-app-*.vercel.app
# Wildcard entries above auto-map to allow_origin_regex; override if needed:
# ALLOW_ORIGIN_REGEX=https://.*\.vercel\.app
ENVIRONMENT=production
AI_SERVICE_REQUIRE_AUTH=true
RATE_LIMIT_PER_MINUTE=60

# Stripe webhook endpoint (Render/Railway URL):
# https://localpilot-ai-service.onrender.com/stripe-webhook
#
# Stripe Dashboard → Webhooks → Events: checkout.session.completed
# After deploy, verify: curl https://localpilot-ai-service.onrender.com/health
# Expected checks.stripe=true when STRIPE_* env vars are set.